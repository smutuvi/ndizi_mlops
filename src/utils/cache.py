# src/utils/cache.py — optional dataset cache hooks (stubs for parity with larger ASR codebases).


def load_encoded_datasets(*_args, **_kwargs):
    return None, None


def save_encoded_datasets(*_args, **_kwargs) -> None:
    return None
